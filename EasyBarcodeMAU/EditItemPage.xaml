<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:EasyBarcodeMAU.Models"
             xmlns:cv="clr-namespace:Camera.MAUI;assembly=Camera.MAUI"
             x:Class="EasyBarcodeMAU.EditItemPage"
             Title="Ürün Değişikliği">
    
     <ContentPage.BindingContext>
     <vm:ReadBaseModel />
 </ContentPage.BindingContext>
    <VerticalStackLayout>
        <Button Text="+" Clicked="ArtirButton_Clicked" />
        <Button Text="-" Clicked="AzaltButton_Clicked" />
        <Label Text="{Binding ReadedCount}" />
    </VerticalStackLayout>
    
</ContentPage>